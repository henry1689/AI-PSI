"""存储 Port 的**契约测试**（ADR-0009，risks.md R13）。

🔴 这里的测试**同一套代码跑两个实现**：内存实现与 PostgreSQL 实现。

存在的理由是一条具体的失败模式：**"阶段 3 通过、阶段 5 爆炸"**。
阶段 3 的场景测试跑在内存实现上；阶段 5 换上 PostgreSQL + pgvector 时，
如果两者的语义有差异（比如删除没传播到索引、乐观锁不报错而是静默覆盖），
问题会在最不该出问题的时候出现，而且看起来像是"新代码的 bug"。

契约测试是唯一能可靠防住这件事的手段：
差异会在**写第二个实现的那一刻**暴露，而不是在替换时。
"""

from __future__ import annotations

__all__: list[str] = []
