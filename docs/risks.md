# 风险清单

> 相关：`docs/implementation_plan.md`、各 ADR
> 更新：每阶段结束复核一次

---

## 1. 认知风险（最高优先级）

这类风险的特殊之处：**后果是累积的**。
一次错误回答只影响一次交互；一次错误的记忆写入会持续影响此后所有回合。

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| **R1** | **记忆污染**：错误信息被写入长期记忆 | 持续污染后续所有回合，且用户难以察觉 | WritePolicy 白名单 + 默认拒绝（ADR-0004）；`memory_write_rejection_rate` 作为观测指标 |
| **R2** | **跨用户泄漏** | 隐私事故，不可撤销 | `user_id` 作用域为必填参数（无默认值）；属性测试断言（硬指标 = 0） |
| **R3** | **删除未传播到向量索引** | "已删除"的记忆仍可被检索出来 | 删除必须同时作用于主表与索引；阶段 5 专项测试 |
| **R4** | **过度自信**：回答比内部判断更确定 | 误导用户，且违反不变量 7 | `EpistemicAction` → 语气档位映射 + 渲染后一致性校验 |
| **R5** | **迎合**：为让用户满意而放弃分析 | 系统退化为奉承机器 | 场景 D 专项；元认知的 `user_pleasing_bias_risk` 检查 |
| **R6** | **把用户赞同当事实验证** | 不变量 4 被绕过 | 反馈路径**没有**"置为 VERIFIED"的入口 |
| **R7** | **抽象逃逸**：用抽象语言掩盖事实不足 | 回答看似深刻实则空洞 | 元认知的 `abstraction_escape_risk`；§9.9 明确禁止 |
| **R8** | **虚假平衡**：机械地"双方都有道理" | 失去判断力 | §9.8 明确禁止；`DialecticalAnalysis` 要求给出真实张力 |
| **R9** | **提案门槛被绕过**：单次经验升级为全局策略 | 违反不变量 10 | 门槛以常量实现并测试；`proposal_false_promotion_rate` 硬指标 = 0 |

---

## 2. 工程风险

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| **R10** | **Prompt 散落在业务代码中** | 无法测试、版本化、回滚 | Prompt 契约规范（`docs/prompt_contracts.md`） |
| **R11** | **三套模型（domain/db/api）耦合** | 层间泄漏，改一处波及三层 | 禁止 `model_validate` 一键互转；显式转换函数 |
| **R12** | **Port 设计不当** | 阶段 5 替换实现时波及场景测试 | Port 形状直接依据任务书 §10 设计，不自创语义（ADR-0009） |
| **R13** | **内存适配器与真实实现语义漂移** | 阶段 5 才暴露行为差异 | ✅ **已建立并在阶段 3 生效**：契约测试同一组断言跑两个实现，当场抓到两处 PostgreSQL 实现的不一致（ADR-0015 §4） |
| **R14** | **事件表无限增长** | 查询变慢 | 阶段 2 处理索引；V0.1 不做归档（规模不需要） |
| **R15** | **乐观锁冲突被当成异常处理** | 无谓失败 | 冲突是正常业务路径，返回可重试的明确错误（ADR-0002） |
| **R16** | **超时配置写死后不可调** | 环境差异导致误判 | 超时表进 `config.py`，可环境变量覆盖（ADR-0008） |

---

## 3. 环境与工具链风险

| # | 风险 | 状态 | 处理 |
|---|---|---|---|
| **R17** | 本机 `make` 缺失 | ✅ 已解决 | winget 装 GNU Make 4.4.1 |
| **R18** | 无法直连 Docker Hub | ✅ 已解决 | `AI_PSI_PG_IMAGE` 可覆盖；本地走镜像源 |
| **R19** | ruff 格式化 Markdown 代码块 | ✅ 已解决 | 排除 `**/*.md`（ADR-0007） |
| **R20** | Python 3.13 依赖 wheel 缺失 | ✅ 未发生 | 实测全部依赖有 wheel；若发生则回退 3.12（ADR-0007） |
| **R21** | shell 的 `VIRTUAL_ENV` 指向全局 3.11 | ⚠️ 仅噪声 | uv 自动忽略并使用项目 `.venv`，不影响结果 |
| **R22** | 网络不稳定（Docker Hub / SourceForge 均曾超时） | ⚠️ 观测中 | 重试；关键依赖提前下载 |
| **R28** | **Windows 事件循环不被 psycopg 异步支持** | ✅ 已解决 | `asyncio_compat` 统一处理；pytest 需 hook + 全局策略**双保险**（ADR-0014） |
| **R29** | **`.ini` 文件被 locale 编码读取** | ✅ 已解决 | `alembic.ini` 保持纯 ASCII（踩了两次：中文注释 + 破折号）（ADR-0014） |
| **R30** | **集成测试误连开发库并 TRUNCATE** | ✅ 已缓解 | 测试库独立派生 + 配置层拒绝两者相同（ADR-0014） |
| **R31** | 测试引擎池化连接跨事件循环复用 | ✅ 已解决 | 测试引擎用 `NullPool`（ADR-0014） |
| **R32** | **反刍检测只有词面精度** | 改写过的同一论点不会被判为重复，导致多跑一轮 | 刻意保守：漏判由循环上限与预算兜住，误判则会让系统提前放弃分析。语义级检测待阶段 5 向量检索。`tests/unit/test_reliability.py` 有一条用例把这个局限**钉死为预期行为** |
| **R33** | **预算表与模块矩阵漂移** | 二者住在不同模块，改一处忘一处，"循环永不超预算"在运行期破功 | 三张表（预算 / 标称调用数 / 模块矩阵）的一致性由 `tests/unit/test_orchestrator_and_depth.py` 断言 |
| **R34** | **记忆写入与事件写入不同事务** | 极端情况下可能出现"记忆改了但事件没写" | ✅ **阶段 5 已闭合**：记忆仓储纳入 `UnitOfWork`，四步同事务。由 `TestAtomicity`（注入一个写事件必失败的存储）作为**直接证据**（ADR-0017 §3） |
| **R36** | **推理 token 吃掉输出预算** | 截断 → 分析丢失甚至回合失败 | 推理预留按实测标定（16384）；截断在解析前判定并给出可操作的错误；可选模块降级而非失败。**根治靠提示词约束输出规模，需评测支撑（阶段 7）** |
| **R37** | **真实模型不按 Schema 说话** | 结构化解析失败 | 提取修复（去代码块 / 括号配平扫描）+ JSON 模式 + 有限重试；失败时可选模块降级并留痕。**实测：真实回合中 `logical_analyzer` 的失败几乎全部是截断而非格式** |
| **R38** | **单回合成本远高于直觉** | D4 实测 ~21k 输入 + ~33k 输出（其中推理 ~22k） | 已量化记录（ADR-0016 §10）；预算表限制调用**次数**，但无法限制单次生成长度——这是阶段 7 的评测对象 |
| **R39** | **同一个旋钮有两个默认值** | 改一个毫无效果，排查成本高 | 配置层默认改为 `None`（= 用 Provider 默认值）；`TestReasoningHeadroomPlumbing` 钉死，含 `0` 必须被当作合法配置 |
| **R40** | **V0.1 没有认证层** | `user_id` 由调用方声明，故意传入别人的 id 就能读到别人的记忆 | 作用域过滤防的是"代码写错导致的串号"，**不是恶意调用者**。边界写在 `api/routes/memories.py` 的模块文档里；认证不在 V0.1 范围内，阶段 8 的"已知限制"必须包含它 |
| **R41** | **默认向量是词面的，不是语义的** | 同义改写匹配不上，用户会觉得"系统明明记过却想不起来" | `LocalHashingEmbedding` 的局限写在模块文档与 ADR-0017 §1；换 `openai_compatible` 即得真语义。**风险在于它看起来"能用"**——检索确实工作，只是召回范围比语义窄 |
| **R42** | **冲突阈值随向量空间漂移** | 换语义模型后 0.75 从"很接近"变成"随便两条都算"，线索变成噪音 | 阈值有明确文档标注"针对本地向量标定"，换 Provider 必须重新标定；线索只进审计、不驱动任何有代价的动作，因此漂移的后果是**噪音而非错误判断** |
| **R43** | **换向量 Provider 后旧记忆静默失效** | 检索只比对同版本向量，换 Provider 后**旧记忆一条都检索不到**，且不报任何错 | 这是刻意的（比较两个不同语义空间的向量毫无意义），但必须可修：`MemoryService.reindex` 显式重建；`test_only_matching_versions_are_retrieved` 复现这个状态 |
| **R44** | **向量维度是数据库列的固定属性** | 改了配置却忘了改迁移，写入会被 PostgreSQL 拒绝 | 维度写死在迁移里（用常量而非运行时配置，保证迁移可复现）；`test_stored_vector_has_the_migrated_dimension` 在真实库上核对；`OpenAICompatibleEmbedding` 对返回维度不符**明确失败** |
| **R45** | **反馈正文留在事件流里，不随"删除用户数据"消失** | 用户以为删掉的东西，其**原话**仍躺在只追加的事件表里 | 这是刻意的：反馈正文是对话记录（事件流里本来就存着 `user.message.received` 的正文），摘掉它这条事件就只剩"用户点过一次纠正"。代价是**删除范围与用户直觉不一致**，因此 `DELETE /users/{id}/data` 的响应与文档都写明"只覆盖记忆"（`api/schemas.py`、`api/routes/memories.py`）。是否要提供"擦除反馈正文"的能力，留给用户决定（ADR-0018 §4） |
| **R46** | **归因偏保守，真实错误可能大比例进不了模式发现** | `UNKNOWN_ERROR` 只在用户明确否定时出现，其余判不了的情况**直接不归因**；模式发现因此可能长期沉默 | 这是刻意选的偏向：归错了比不归因更糟（把"当时信息本就不足"判成"推理错误"，系统会学到"这类问题要更保守"，而真正的教训是先取证）。**代价是漏报**。阶段 7 应统计"有多少真实错误根本没进模式发现"，用数据决定要不要放宽判据（ADR-0018 §10） |
| **R47** | **提案的 `possible_regressions` 是模板文字，不是计算结果** | 评审看到一段像分析的文本，误以为系统已经算过影响面 | 字段写的是**通用风险模式**（"针对该情境的改动可能让其他情境变差"），不含任何该提案特有的计算。真正的退化预测需要 Golden Dataset（阶段 7）。模板措辞刻意停在方向上，不给具体改法（ADR-0018 §10） |
| **R48** | **内存实现的乐观锁只在"可见版本"上成立，真交错下会静默丢更新** | 🔴 阶段 6.5 §八 评审 C 实测推翻了本条的初版措辞（原文写的是"内存工作单元的方法没有 await 点，所以**测不到**交错"——那是把它说成了观测局限，而实测是**真没有防护**）：`save()` 的检查读"已提交 + 本事务暂存"，两个事务都读到 v1、都通过检查、都提交时，**后提交者静默覆盖前一个**，没有任何异常。PostgreSQL 不会这样，因为 `UPDATE ... WHERE version = ?` 是原子的 | **已修**：`InMemoryStore.apply` 在锁内、**任何写入之前**按 `ExpectedVersions` 复核已提交版本，冲突抛 `OptimisticLockError`；回合 / 记忆 / 提案三条 `save` 路径都接了。四条用例钉住（`tests/unit/test_in_memory_optimistic_lock.py`），并实证过"关掉复核即变红"。⚠️ **仍存的边界**：当前生产路径的临界区内没有 await 点，因此这条缺陷在今天的配置下不会自然发生——修的是"一旦出现 await 就会开始丢数据"这个**将来**的形态。另：契约测试里 Round / Memory 的过期写入此前用的是**凭空造**的版本号（`+5` / `+3`），已按 `ProposalRepositoryContract` 的形状改成真实旧版本，本条的措辞随之成立 |
| **R49** | **反馈是 V0.1 里唯一能把任意自由文本写进记忆的生产路径** | 内容层唯一防线是关键词匹配，而 `write_policy.py` 自己声明"关键词匹配必然漏"；反馈硬编码 `sensitivity=PERSONAL`（恰好等于自动写入上限），因此敏感度闸门对它也不生效 | 写入仍必须过 `WritePolicy`（ADR-0004 的白名单、内容红线、确认要求），且记忆标记为 `SELF_REPORTED` 并记录来源反馈事件。**但"用户自己写下并明确要求记下来"这件事本身是正当的**——真正的缺口是内容层没有比关键词更强的判据。列入阶段 7 的评测范围（ADR-0019 §2.8） |
| **R50** | **失败回合不产生经验** | "学习链路只接在 `_close_round`（成功收尾）上"，而失败回合恰恰是最该学的东西 | 失败回合的 `failure_stage` / `error_category` 已经落在回合投影与事件里（不变量 20），信息没丢；缺的是把它转成 `Experience` 的那一步。**这是阶段 7 的显式待办**——归因判据里的 `_from_failure` 分支目前在生产上不可达 |
| **R51** | **学习链路仍有无约束的自由文本通道** | `applicable_conditions` / `counterexamples` / `situation_signature` / `failure_stage` / `fix_direction` 都是自由 `str`；调用方往里填用户原话，它会一路进 `Experience`、`ErrorPattern`、提案的 `applicability` 并持久化 | `RoundRecord` 的字段集合被测试钉死（挡得住"新增一个 `user_message` 字段"），但那**挡不住"往已有字段里填原文"**。残余风险已在两处显式钉成用例（`test_the_field_set_test_is_not_enough_on_its_own` 等）；运行时**不**把 `Judgment.strongest_counterarguments` 填进 `counterexamples`。要变成机制需要长度/格式约束，收益与代价尚未评估（ADR-0019 §3.1） |
| **R35** | **提示词注入** | 用户内容里的三连反引号可以提前闭合数据块，把后续文字变成"指令" | 数据块内容经 JSON 编码后**额外转义反引号**；模板只允许 `{{input}}` 一个占位符；`tests/unit/test_prompt_registry.py` 有用例专门复现这个攻击 |
| **R52** | 🔴 **I01 与 I13 的运行期强制实际为零** | `Hypothesis.is_factual_claim`、`can_be_written_as_fact`、`UserModel.is_confirmable` 都是**常量 `False`**，因此 `assert_hypothesis_not_fact` 与 `assert_user_model_not_confirmed` 的 `raise` 分支**不可达**；这两个守卫在 `src/` 里也**没有任何调用者** | 它们不是防线，是**写在代码里的文档**。真正的保证在类型层（属性没有为 True 的可能）。阶段 6.5 §五.4 要求"每条不变量一个定向反例"，而这两条**造不出反例**——用 monkeypatch 造一个只能证明"monkeypatch 好使"。真实情况被钉在 `tests/unit/test_invariant_counterexamples.py::TestI01AndI13CannotBeBrokenByConstruction` 里：将来属性若被改成真实判断，那条用例会红，改动者必须同时回答"现在的运行期强制是什么" |
| **R53** | **无幂等键的重发会被算作"又发生了一次"** | 客户端不带 `Idempotency-Key` 地重发同一句话，服务端盖一个新的回合，`independence_group` 因此不同，门槛把它算成两次独立发生 | 这是**已知边界**，不是遗漏：要让它塌缩成一次只能靠比较消息正文，而那是**语义判断**，不是本系统做归因的方式（ADR-0020 §2）。带幂等键的重试与事件重放都已被正确处理并有测试 |
| **R54** | **经验的评价只升不降** | `assess_experiences` 取最高一档，而 V0.1 没有"推翻一次确认"的评价路径。一次错误的用户确认会把一条经验**永久**抬到 `CONFIRMED` | 取最高档是刻意的（"最后一条说了算"会让确认被随后的 `SUSPECTED` 无声降级）。降级需要**显式引入一种表达否定评价的评估者**，那时 `assess_experiences` 的合并规则要一并改。在 V0.1 的规模下（经验以百计）影响有限 |
| **R55** | **被裁决过的模式不会再被提议** | 一条提案被驳回后，同一个 `(错误类别, 情境签名)` **永远不会**再生成新提案——即使此后又发生了很多次 | 阶段 6.5 §四 修掉的是反方向（原本会"每跑一次再递一遍"）。这个取舍是显式的：V0.1 没有"重新开启"机制，而"反复递同一份"对评审的伤害更直接。引入重新开启（例如"驳回 N 天后、或新增 M 次发生时可再审"）属于阶段 7，那时改的是 `_covered_keys` 的一行 |
| **R56** | **`evidence` 的长度上限写在 Schema 校验器里而不是 `Field(max_length=...)`** | 同一份 `schemas.py` 里，单值的长度上限用 `Field`、列表项的长度上限用 `field_validator`，两种写法并存 | 原因是 pydantic 的 `max_length` 对 `list` 管的是**条目数**不是**各项长度**，逐项长度只能在校验器里做。风险是改动时容易只改一处。边界行为由 `tests/integration/test_input_contract.py` 在两个后端上钉住 |
| **R57** | **`ProposalGate` 每次复核都要全量读取事件流** | 代价随历史线性增长；经验数量大时一次学习运行会扫很多遍 | V0.1 的经验总量以百计，实测（1713 用例全套跑 113 秒）没有成为瓶颈。阶段 7 若经验量上升到需要分页，读取器需要一个按 `(错误类别, 情境签名)` 的索引——那时改的是 `ExperienceReader`，门禁一行不用动（ADR-0022 影响一节） |
| **R58** | **归因判据里仍有三条在生产上不可达** | `_from_failure`（失败回合不构经验）、`_from_budget`（刻意不喂）、`_from_memory_rejection`（无生产者） | 与 R46/R50 同源。⚠️ **阶段 6.6 已接通第四条**——它不再是"用户纠正无回流路径"，而是新的 `_from_user_correction`（ADR-0023），并且在默认配置下**真的会被走到**（黑盒 A–H 实测）。剩下的三条需要失败回合产生经验（R50）与记忆拒绝回流，都是阶段 7 的显式待办 |
| **R69** | **落在"曾被有效纠正过"的回合上的🔴 *任何*反馈都会触发一次完整的学习运行** | ⚠️ **触发面比"有依据的纠正"宽得多**（评审 6.6 §F3 实测）：一条 `acknowledgement`（"收到，谢谢"）落在这样的回合上，同样会拉起一次全量学习链路——读全部 `experience.created` / `evaluated` / `attributed` / `user_feedback_received` 与全部回合度量，外加每个模式一次门禁重读。V0.1 没有限流，因此客户端可以在那个回合上反复点赞，每次换一次全量扫描 | 触发条件**刻意**选成"本次反馈之后该回合**存在**有效归因"而不是"本次新写了归因"——用"可能多跑几次"换"绝不永久卡死"（ADR-0023 §6）：后者会让"学习链路偶发失败 + 客户端重试"这条链路**永久**停在没有提案的状态。V0.1 规模下（经验以百计）不是瓶颈；阶段 7 若经验量上升，读取器需要按 `(错误类别, 情境签名)` 建索引（同 R57），触发器可加一个"本次反馈是否构成新归因"的短路 |
| **R70** | **归因冲突在 V0.1 没有裁决入口** | 一条经验拿到两套互相矛盾的归因后，`effective_error_type` 会是 `None` 并**一直**是 `None`——它不计入任何门槛，直到有人手工处理。而当前**没有接口或 UI** 去解决它 | 这是刻意的取舍（"冲突解决前不计数"，ADR-0023 §5）：自动挑一个会让矛盾不可见，比暂时不计入危险得多。冲突条数在 `PatternScan` / 学习运行响应 / `GateVerdict.data_quality` 三处公开，因此它**不会被静默忽略**。真正的裁决入口属于阶段 7 |
| **R71** | **纠正推出的错误类别是"约定"，不是"测量"** | 六条映射规则（假设无证据 → `evidence_error` 等）是 V0.1 的设计决定，不是对错误本质的独立观测。把它当成客观分类，会让下游以为这些类别的可信度与阶段映射（`STAGE_ERROR_CATEGORY`）相同 | 置信度保持 `MODERATE`，但**理由串、字段说明与 ADR-0023 §3 三处都逐字写明**：这是「用户明确纠正」+「结构映射规则」两条**非独立**依据形成的**策略性归因**。`classifier_version` 随每条归因落档，规则一改就能追出历史归因是哪一版给出的 |
| ~~R72~~ | ✅ **已关闭**（阶段 7 第一项）——并发的两次纠正写出两条一模一样的 DRAFT 提案 | 原文：`_covered_keys()` 是"先读再生成"，读与写之间没有锁、表上也没有对应唯一约束；评审 6.6 §F1 实测间隔 < ~50ms 的两个纠正请求各读到空集，各写一条 | **处置**：部分唯一索引 `uq_improvement_proposals_active_pattern`（`(error_class, (applicability[1]))`，排除空 applicability 与终态）——唯一性由 PostgreSQL 裁决，`_covered_keys()` 降级为快速路径。ADR-0024。⚠️ **关闭的证据是反向验证**：`DROP INDEX` + `pg_indexes` 复查后跑并发用例 → 真的写出 2 条、用例变红；恢复索引后变绿。🔴 **降级会重新打开它**，见 R76 |
| **R73** | **并发的同一次纠正可以写出重复的 `experience.attributed` 事件** | `_attribute_experiences` 里的去重是"先读 `already`、再写事件"，两者之间没有锁。评审 6.6 §F2 实测：同一回合、同一指针的 N 条并发纠正下，事件条数为 1（N=2）/ 2（N=4）/ 4~5（N=8） | ⚠️ **它不抬门槛**：读取端 `_attributions_for` 按**同一个三元组** `(experience_id, error_type, related_artifact_id)` 去重，`_effective_attribution` 用集合判类别，因此有效类别与加权计数都不变；`independence_group` 更是与归因条数无关。受影响的是**审计面的原始事件条数**。方向同 R72：要么在 `events` 上加一条按负载的去重约束，要么承认它是**尽力去重**而不是保证——代码注释已按后者改写 |
| **R74** | 🔴 **"跨主体"的绑定不是结构保证：V0.1 全仓库没有鉴权** | `_resolve_correction_target` 只在本回合的事件流里解析指针，因此**跨回合**是结构保证（另一回合的产物根本不在候选里）；但**跨主体**不是——`api/routes/` 里没有任何认证依赖，`FeedbackService` 读了 `round_.user_id` 却从不与调用方身份比对。任何知道 `round_id` 的调用方都能在**别人的回合**上发反馈并写归因 | 这是 V0.1 的**全局**性质（不是阶段 6.6 引入的），但本阶段新增的那句文档说"别的用户/租户……根本不在这张表里"**比事实强**——它对**记忆**成立（另一张表），对"别人的回合产物"不成立。文档已按事实改写。鉴权属于阶段 7 或更晚 |
| **R75** | **`ErrorPattern.experience_count` 恒等于 `occurrence_count`，报不出"重复抽取造出的冗余"** | 两个构造点（`_build_pattern` / `_build_suppressed`）拿到的都是**去重之后**的集合，因此"它 ≥ occurrence_count，两者的差就是冗余"这句文档描述的信号**不存在**——差值恒为 0 | 评审 6.6 §F4。⚠️ 它**不影响门槛结果**（门槛数的是加权计数），因此变异测试测不到：那条分数度量的是"测试对代码改动有多敏感"，而这个字段的**规格**从来没有被编码进任何测试。文档已按事实改写（**未实现该信号**）：它在 `pattern_detector` 的汇报面上，与归因链路无关，且没有任何生产消费者——实现它要改 3 处既有测试的期望值，属于下一阶段该单独决定的事（要么实现、要么把这个字段删掉） |
| **R78** | 🔴 **生产路径没有 Evidence 输入，导致归因规则「假设 / 有支持证据 → `REASONING_ERROR`」不可达** | `SubmitMessageRequest`（`api/schemas.py`）**没有 evidence 字段**，`api/routes/conversations.py` 构造 `RoundRequest` 时也不传它——因此生产路径上 `RoundRequest.evidence` **恒为空元组**。连锁：`hypothesis_generator.evaluate` 的 `supporting` 恒空 → `Hypothesis.supporting_evidence_ids` 恒空 → 阶段 6.6 的 `CorrectionTarget.has_supporting_evidence` 恒为 `False` → ADR-0023 §2 的规则 #3 **永不触发**，恒走规则 #2（无支持证据 → `EVIDENCE_ERROR`）。🔴 **独立佐证**：阶段 7 的并发验收 20 轮里，每一轮的情境签名都是 `d2\|no_evidence\|h2`——`evidence_bucket` 恒为 `no_evidence` | **影响**：`EVIDENCE_ERROR` 可能被**系统性多产**、`REASONING_ERROR` **少产**。这不只是分类偏好问题——**R46**（归因偏保守、真实错误漏报）与 **R58**（三条判据不可达）的评测都建立在这两个类别的比例上，而其中的分母本身是偏的。**当前处置：保留风险，不改归因实现**。等正式阶段 7 的 Golden Cases 与归因指标提供数据后，再决定是①给请求面补 evidence 输入，②把规则 #3 从映射表里显式移除并记为设计取舍，还是③维持现状并在评测报告里注明该分支未生效。⚠️ **不要**据此认为规则设计有问题——规则本身没有实现缺陷，是它的**输入条件在当前接口下拿不到**（全阶段审计 2026-09-19 发现，ADR-0023 §2 有对应注记） |
| **R76** | 🔴 **降级（`downgrade`）会静默重新打开 R72** | `alembic downgrade` 只是 `DROP INDEX uq_improvement_proposals_active_pattern`。降级之后并发的两个学习运行又能各自写出内容相同的 DRAFT——**而没有任何测试会自动变红**：并发用例只在有人主动跑它时才跑，且它此时确实会红 | 这是**如实标注**而非缺陷：schema 回退本来就意味着放弃它承载的保证。之所以写进风险表，是因为这条保证的失效是**静默**的——没有任何运行时信号说"唯一性没了"。缓解：并发用例里有 `test_the_index_exists_and_is_partial`，它把索引形状与设计逐字钉住，任何人在**跑测试**时会立刻看到；降级应当被当作"确认要放弃这条保证"，而不是一次无副作用的回退（ADR-0024 §5） |
| **R77** | **`find_active_for_pattern()` 的"多于一条就抛"分支正常不可达** | 唯一索引保证至多一条，因此那条 `ConstitutionViolationError` 只在**索引被人删掉**（或迁移没跑）时才会触发。它没有走共享契约（后端根本构造不出那个状态） | 它是**防御性**代码，靠"直接构造损坏状态"来测：内存侧在 `tests/unit/test_in_memory_pattern_uniqueness.py` 里绕过仓储直接往 store 塞两条；PostgreSQL 侧只能通过删索引复现（反向验证脚本正是那个场景）。保留它的理由：真出现两条时"挑一条返回"会让调用方以为一切正常，而唯一性其实已经没了——那是**只在并发下才暴露**的缺陷，永远不会被日常使用发现 |
| **R59** | 🔴 **§二.4 那条硬线依赖"调用方传的是枚举成员"** | `assert_evaluator_may_produce` 用 `evaluator is ExperienceEvaluator.INTERNAL_METACOGNITION` 判断。**如果**一个非成员值（例如从 JSON 直接取出的裸字符串 `"internal_metacognition"`）到达这里，`is` 会判为「不是内部元认知」——于是**静默放行自我确认**，而 `==` 会正确拦住 | **当前不可达**：该函数的两个调用点都来自 pydantic 模型字段（`ExperienceEvaluator` 标注，pydantic 会把 JSON 字符串**强制转换**成成员），mypy 也在调用点强制类型。它被记下来是因为失效方式是**静默地反向**——类型纪律一旦在某处松动（例如将来加一个绕过 pydantic 的适配器），这条不变量不是"失效"而是"变成放行"。加固只要三行（进来先 `isinstance` 断言），但没有真实路径触发它，因此留作显式残余风险而不是现在就加一个不可达分支 |

> **以下 R60–R65 来自阶段 6.5 §八 的独立评审。** 它们全部是
> **评审主动构造出反例之后**才被记下来的——也就是说，在记录它们之前，
> 每条都有一个可运行的攻击路径。

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| **R60** | 🔴 **PostgreSQL 上声称的 `canonical_key` 部分唯一索引从未建立** | ADR-0020 §2 与 ADR-README 的 G19 都写了 `CREATE UNIQUE INDEX ... ON events ((payload->'experience'->>'canonical_key'))`，**读起来像已经建好了**。实测 dev/test 两个库该索引数均为 0，迁移链 head 里没有任何一个迁移提到它 | 处置是**把文档降级为设计意向**（ADR-0020 §2），而不是补建索引。理由：门槛的计量单位是 `independence_group` 而非 `canonical_key`（同键必同组，在计数端已经塌缩），而 PG-only 约束会让两个后端在 `tests/contract/` 的同一组断言上分家。后果限于**存储冗余**：同一回合的经验可被写入两次，`experience_count` 与 `occurrence_count` 的差会变大——系统把两者都报出来正是为了让冗余可见 |
| **R61** | 🔴 **`GateVerdict` 的"第二道防线"只挡随手构造，不挡进程内伪造** | ADR-0022 §3 称真正的防线是"必须同时伪造一份达标的 `ErrorPattern` 与准许的 `PromotionDecision`"。评审实测：`dataclasses.replace`（**把 `_token` 原样带过来**，且是**正常库调用**）、`object.__new__`、`copy.copy`、`deepcopy`、`pickle`、子类化——**全部绕过**。而 `ErrorPattern` / `PromotionDecision` 是零校验的裸 frozen dataclass，"达标"退化成手打一个数字 | 进程内防伪在 Python 里**做不到**，因此不假装做到：ADR-0022 §3 已按实测逐条更正。真正的保证是调用纪律（落库必须携带 `GateVerdict`，且它只能由 `ProposalGate.review()` 从仓储重查重算后产出）加上可复核的审计数字。**能在进程内执行任意 Python 的主体，已经越过了本系统的边界** |
| **R62** | **仅靠伪造事件流即可走完学习链路** | 6 条伪造事件（3 条 `experience.created` + 3 条伪造的"用户纠正"评价，全部引用同一个幽灵 UUID）→ 正式学习入口 → 生成并落库提案。全程没有伪造 `GateVerdict`，也没有任何真实用户参与 | 比 R61 **更难被代码评审拦住**：它用的全是**合法的写入原语**。唯一可能的防线是"事件流里的经验必须能追到真实的回合"，而 `origin_event_ids` 虽然存了锚点，**没有任何东西去核对它**（与 R63 同源）。V0.1 不引入跨事件核对（代价与误拒风险需单独评估），按残余风险记录 |
| **R63** | **`evidence_refs` 从不与事件流核对** | `ExperienceEvaluationRecord._check_confirmation_needs_evidence` 只拦**空列表**。引用一个不存在的证据 uuid 照样被接受，于是"SUPPORTED / CONFIRMED 只能来自系统之外"这句话在数据层**没有可证伪的落点** | 与 R62 互为表里：两者都是"事件流里的断言没有被交叉核对"。真正的核验要遍历事件存储逐条比对，代价是每次学习运行多一遍全量读；误拒风险（保留策略裁剪掉早期证据）尚未评估。V0.1 记风险 |
| **R64** | **`ProposalGate` 的权重表是公开构造参数** | `ProposalGate(reader, weighting=…)` 接受任意权重表，而校验只有"非负 + 非递减"。注入一张 `SUSPECTED = 1` 的表即可让**内部怀疑**参与计权，绕过 §二.6–7 那条"内部怀疑不是证据"的规则 | 这是**依赖注入的固有代价**，不是缺陷：生产路径上唯一被注入的是 `DEFAULT_WEIGHTING`（组合根固定），`tests/unit/test_evaluation_weighting.py` 对默认表逐项断言。记下来是因为"默认值是对的"与"改成别的会被拦"是两件事——**后者不成立** |
| **R65** | **`extractor_version` 是自由字符串，同一回合可产出无限多个 `canonical_key`** | `Experience` 的一致性校验器逐字核对 `canonical_key` 与四个来源，但 `extractor_version` 本身没有上界——传 `"v1"` 与 `"v2"` 就能让同一回合同一判断得到两个不同的键 | ⚠️ **它不抬高门槛计数**：门槛数的是 `independence_group`（由幂等键 / 回合 id 决定，与 `extractor_version` 无关），两条不同键的经验仍落在同一分组里、仍塌缩成一次发生。钉死版本号会让"版本升级后回放旧事件"**全部读不回来**，代价远大于收益。等真的需要"一条经验只有唯一一份"时，再引入历史版本登记表 |
| **R66** | **反馈事务内做外部 IO，且有一次无上界的全表扫** | `MemoryService._write_in` 的重复检查走 `uow.memories.retrieve()`，其中会调 embedding provider——**事务保持打开期间可能发生网络调用**（真实 provider 时）。`_current_evaluations` 调 `read_by_event_type(EXPERIENCE_EVALUATED)` **不带 limit**，每次反馈都在事务内扫全库的评价事件 | 与 R57 同类（代价随历史线性增长，V0.1 规模下不是瓶颈）。事务内 IO 的真实影响取决于 provider 延迟，**当前没有性能反例**——它是**读码推测**，不是实测。阶段 7 若引入分页或真实向量 provider，这条要重新评估 |
| **R67** | **只由零宽字符组成的值仍能进只追加事件流（部分字段）** | §二 的 `is_blank` 应用点清单里没有 `SubmitMessageRequest.response_style` 与 `FeedbackRequest.related_claim`。评审 C 实测：`response_style="​"` 与 `related_claim="​​"` 在两个后端上都 **201**，值原样落库 | ⚠️ **两个后端结果一致**，因此它不是契约分叉，**不构成过度声称**（ADR-0021 §2 的应用点清单从未包含这两个字段）。它与 §2 要防的是同一件事（"看起来是空的"记录进删不掉的流），但影响面小得多（不是正文，不会进记忆）。阶段 7 若要收口，做法与 §2 相同：把它们加进 `_reject_if_blank` 的应用点 |
| **R68** | **双后端契约测试只比对状态码与 `code`，不比对响应体** | `tests/integration/test_input_contract.py::_assert_client_error` 只查三件事：4xx、有 `code`、`code != internal_error`。**字段路径、message 文本两边不同都不会红**——文件 docstring 自己承认了这个取舍 | 这是**有意的松紧取舍**（把整个响应体逐字比对会让任何措辞改动都变成契约破坏）。代价是：只影响响应体形状的分叉抓不到。**缓解**：现阶段真正的分叉（F1 长度、F2 NUL）都表现为状态码不同，会被参数化用例抓住——但那是**运气**，不是机制。阶段 7 若要收口，做法是给少数关键字段加"两边取值必须相同"的显式断言 |

---

## 4. 范围风险

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| **R23** | **范围蔓延**：顺手实现下一阶段的功能 | 地基未稳就盖高层，返工成本高 | ADR-0012 明确边界；每阶段开工清单 |
| **R24** | **为通过检查而降低标准** | 质量门槛失效 | 明确禁止删测试/放宽 Schema/加 `# type: ignore`；任何豁免需 ADR |
| **R25** | **"留接口"变成"建空壳"** | 大量 import 失败的包，误导读者以为能力已存在 | 只为当前阶段有内容的模块创建包（ADR-0012） |
| **R26** | **宪法被静默修改** | 边界失效且无人察觉 | 宪法即代码，修改必留 git diff（ADR-0011） |
| **R27** | 任务书自身矛盾未被发现 | 实现到一半才发现 | 已做全量冲突检查（20 项）；后续发现**新增 ADR**，不就地改 0012 |

---

## 5. 已识别的任务书内部矛盾（已处理）

| # | 矛盾 | 处理 | 状态 |
|---|---|---|---|
| C1 | `EntityMetadata` 要求与 13 个类定义不一致 | 全部继承基类 | ✅ ADR-0006 |
| C2 | 阶段 3 场景 F/J 依赖阶段 5 的记忆系统 | 内存适配器 + 契约测试 | ✅ ADR-0009 |
| C12 | `EpistemicAction` 与 `MetacognitiveDecision` 语义重叠 | 拆分为内部控制流 vs 对外建议 | ✅ ADR-0010 |
| C13 | `DEGRADED` 不在回合状态枚举中 | 归入系统健康状态 | ✅ ADR-0012 |

**其余 16 项缺口**（`related_goal_ids` 无 Goal 实体、`Hypothesis.category` 无枚举、
状态超时无数值、元认知调用是否计入预算、`tests/golden/` 与 `evals/datasets/` 职责重叠、
宪法不可测、无用户创建接口等）默认值见 ADR-0008 / ADR-0012。

---

## 6. 需要持续关注的三个问题

1. **Port 设计**（R12）——✅ 阶段 3 已定型。
   `MemoryRepository` 的形状直接依据任务书 §10 的操作清单，未自创语义。
   阶段 5 的验证点是：换实现时**场景测试一行都不用改**。
2. **契约测试**（R13）——✅ 已建立并立刻证明了自己的价值
   （抓到两处 PostgreSQL 实现的不一致）。
   阶段 5 要做的是把记忆仓储接进 `tests/integration/test_contract_postgres.py`
   里预留的占位类，而不是新写一套。
3. **范围纪律**（R23）——每个阶段结束时仓库必须可运行；
   不可运行就不算完成。
4. **反刍检测的精度**（R32）——当前是词面相似度，只捕捉逐字重复。
   阶段 4 接入真实模型后这个局限已经暴露：真实模型的两次判断会改写措辞，
   因此阈值 0.85 实际上只能捕捉"逐字重复"。
   阶段 5 交付了向量检索的**能力**（`memory/retrieval.py`），但阈值仍是词面标定，
   且默认向量本身就是词面的（R41）——换成外部语义向量之前，
   用它做语义级检测没有意义。**阶段 7 应结合评测数据重新标定阈值**，
   并在此之前决定要不要换向量 Provider。
5. **单回合成本**（R38）——D4 回合实测约 21k 输入 + 33k 输出，其中推理占 22k。
   预算表限制的是**调用次数**，管不住**单次生成的长度**。
   阶段 7 的评测应把"每个任务的合理输出规模"一并标定，
   并把结果反馈到提示词模板里（限定列表长度与条数）。
