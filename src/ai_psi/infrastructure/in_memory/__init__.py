"""内存适配器（ADR-0009）。

🔴 **这不是"简化版实现"，而是与 PostgreSQL 实现共享同一套契约测试的实现。**

它存在的原因是阶段 3 的验收要求"不使用外部 API 也能完整运行"，
且场景 A–J 不应该被数据库拖慢。阶段 5 换上 PostgreSQL + pgvector 时，
**场景测试一行都不用改**——它们只依赖 Port。

内存实现必须满足与真实实现**完全相同的语义**：

* 事务边界：未提交即丢弃（对应 SQL 的回滚）；
* 只追加事件：本包不提供任何修改或删除事件的方法；
* 序号单调：回滚后序号**跳过而不是回退**——
  真实数据库的序列也是这么做的，把它实现成"回退"会让契约测试
  在 PostgreSQL 上失败，而那正是契约测试要防的事；
* 作用域隔离与删除传播：与真实实现同一条不变量（I14 / I15）。
"""

from __future__ import annotations

__all__: list[str] = []
